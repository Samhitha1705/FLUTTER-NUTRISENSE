import 'package:flutter/material.dart';

class Mealsscreen extends StatelessWidget {
  const Mealsscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Your Meals is here!!"),
    ));
  }
}